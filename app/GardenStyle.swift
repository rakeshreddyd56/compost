//
//  GardenStyle.swift
//  Compost — calm design tokens
//

import SwiftUI

// Calm aesthetic: material background, sage accent, dense utility
enum GardenStyle {
    static let accentGreen = Color(red: 0.2, green: 0.55, blue: 0.35)
    static let sageBackground = Color(red: 0.92, green: 0.95, blue: 0.92)
    static let materialGray = Color(red: 0.95, green: 0.95, blue: 0.96)

    static let cornerRadius: CGFloat = 8
    static let spacing: CGFloat = 6
}
